#!/bin/bash

# Get the mode of olleh
olleh_mode=$(stat -c "%a" olleh)

# Set the mode of hello to olleh's mode
chmod $olleh_mode hello

echo "hello's mode is now set to $olleh_mode, matching olleh's mode."
